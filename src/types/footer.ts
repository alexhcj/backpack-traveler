export enum EFooter {
  FULL = "full", // with top block: world map, socials & newsletter
  SHORT_BORDER = "short-border", // only bottom with padding top + border
  SHORT = "short", // only bottom
}
